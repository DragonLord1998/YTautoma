# YouTube Shorts Automation - Local Offline Models
# This setup uses fully local/offline AI models

# Core
requests>=2.31.0
pydantic>=2.0.0
python-dotenv>=1.0.0
Pillow>=10.0.0

# ===========================================
# Z-Image (Local Diffusers)
# ===========================================
# Install diffusers from source for Z-Image support:
# pip install git+https://github.com/huggingface/diffusers
torch>=2.4.0
transformers>=4.40.0
accelerate>=0.27.0
safetensors>=0.4.0
sentencepiece>=0.2.0

# ===========================================
# Wan 2.2 (Local Video Generation)
# ===========================================
# Clone Wan 2.2 repo separately:
# git clone https://github.com/Wan-Video/Wan2.2.git
# pip install -r Wan2.2/requirements.txt
einops>=0.7.0
imageio>=2.33.0
imageio-ffmpeg>=0.4.9

# ===========================================
# VibeVoice TTS (Local)
# ===========================================
# Clone VibeVoice repo separately:
# git clone https://github.com/microsoft/VibeVoice.git
# cd VibeVoice && pip install -e .
soundfile>=0.12.0

# ===========================================
# Optional: Flash Attention (faster inference)
# ===========================================
# pip install flash-attn --no-build-isolation
